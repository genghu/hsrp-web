# Server Configuration
PORT=3000
NODE_ENV=production

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/hsrp
# For MongoDB Atlas, use: mongodb+srv://username:password@cluster.mongodb.net/hsrp?retryWrites=true&w=majority

# JWT Secret (Generate a secure random string for production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS Configuration (Optional - comma-separated list of allowed origins)
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com
